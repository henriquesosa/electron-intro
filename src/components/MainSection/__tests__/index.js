jest.unmock('./')

import React from 'react'
import ReactDOM from 'react-dom'
import TestUtils from 'react-addons-test-utils'
import MainSection from './'

describe('MainSection Component', () => {
	it('shows a title', () => {

		// const MainSection = TestUtils.renderIntoDocument(
		// 	<MainSection />
		// )

		// const MainSectionNode = ReactDOM.findDOMNode(MainSection)

		// expect(MainSectionNode.textContent).equals('Todos')
	})
})