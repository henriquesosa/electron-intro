jest.unmock('./')

import React from 'react'
import ReactDOM from 'react-dom'
import TestUtils from 'react-addons-test-utils'
import TodoTextInput from './'

describe('TodoTextInput Component', () => {
	it('shows a title', () => {

		// const TodoTextInput = TestUtils.renderIntoDocument(
		// 	<TodoTextInput />
		// )

		// const TodoTextInputNode = ReactDOM.findDOMNode(TodoTextInput)

		// expect(TodoTextInputNode.textContent).equals('Todos')
	})
})